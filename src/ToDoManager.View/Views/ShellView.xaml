<UserControl x:Class="ToDoManager.View.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="400" Width="650">
    <Grid>
        <
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border BorderBrush="CadetBlue" CornerRadius="3" BorderThickness="2" Margin="3" Grid.Column="0">
            <ContentControl x:Name="TaskGroupVM" />
        </Border>
        <StackPanel Orientation="Vertical" Grid.Column="1">
            <Border BorderBrush="CadetBlue" CornerRadius="3" BorderThickness="2" Margin="3">
                <ContentControl x:Name="EditTaskVM" />
            </Border>
            <Border BorderBrush="CadetBlue" CornerRadius="3" BorderThickness="2" Margin="3">
                <ContentControl x:Name="EditGroupVM" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>